# 函式 def
函式就像是一個「自動販賣機」或「黑盒子」。你給它一些輸入（投幣/選擇），它會幫你執行特定的任務，最後可能給你一個結果（飲料）。</br>
使用函式的優點：</br>
- 程式碼重用：寫一次，用多次。
- 易於維護：如果要修改邏輯，只需要改一個地方。
- 結構清晰：把複雜的問題拆解成多個小任務。

## 基本語法
在 Python 中，我們使用 def 關鍵字來定義函式：
```
def 函式名稱(參數1, 參數2):
    # 函式內部的程式碼塊
    實作內容
    return 回傳值
```

## 無參數、無回傳
```
def say_hello():
    print("你好！歡迎學習 Python。")
    print("這是你的第一個函式。")

# 呼叫函式
say_hello()
```

## 帶有參數的函式
```
def calculate_area(width, height):
    area = width * height
    print("長度為", width)
    print("寬度為", height)
    print("計算出的面積為", area)

# 呼叫函式並傳入不同的數值
calculate_area(10, 5)
calculate_area(7, 3)
```

## 帶有回傳值 (Return)
```
def add_numbers(a, b):
    result = a + b
    return result

# 取得回傳值並存入變數
total = add_numbers(100, 250)

# 現在我們可以使用這個 total 變數做其他事
print("總和是", total)
print("總和的兩倍是", total * 2)
```

## 預設參數值 (Default Arguments)
可以為參數設定「預設值」，如果呼叫時沒有提供該參數，就會使用預設的內容。
```
def greet(name, message="你好"):
    print(message, name)

# 情況 1：提供所有參數
greet("YTLee", "早安")

# 情況 2：只提供 name，message 使用預設值
greet("小明")
```


# 自動化數據清理機器人

## 目標
利用 Python 與 Pandas 建立一個能自動偵測、清洗並匯出乾淨資料的處理流程。

## 課程複習

### pandas複習

請先把[sales_data.csv](https://drive.google.com/file/d/1-WycmIGry8l67VRVvEe7TmvfalP6ziwZ/view?usp=sharing)下載，</br>
並在雲端硬碟根目錄下的'data'資料夾，將檔案放在'data'資料夾中。</br>

這份資料包含了：
- 缺失值（NaN）
- 重複資料（訂單 ID 1001 重複）
- 格式不一（日期與金額）

```
import pandas as pd
from google.colab import drive

# 1. 掛載 Google Drive
drive.mount('/content/drive')
```
```
# 2. 設定檔案路徑 (請根據您檔案在雲端硬碟中的實際位置修改)
# 假設檔案放在雲端硬碟根目錄下的 'data' 資料夾
file_path = '/content/drive/My Drive/data/sales_data.csv'

# 3. 讀取 CSV
df = pd.read_csv(file_path)

# 查看前 5 列資料
print("原始資料：")
print(df.head())
```

1. 刪除重複資料 drop_duplicates()
```
# 檢查並刪除完全重複的列
df_cleaned = df.drop_duplicates()

print(f"刪除前列數: {len(df)}, 刪除後列數: {len(df_cleaned)}")
```

2. 處理缺失值 - 直接刪除 dropna()
```
# 只要 Date 欄位是空的，就刪除該列
df_dropped = df_cleaned.dropna(subset=['Date'])

print("刪除 Date 缺失後的資料：")
print(df_dropped.info()) # 觀察 Non-Null Count
```

3. 處理缺失值 - 填充 fillna()
```
# 1. 將 Amount 缺失值填補為 0
df_cleaned['Amount'] = df_cleaned['Amount'].fillna(0)

# 2. 將 Product 缺失值填補為 'Unknown'
df_cleaned['Product'] = df_cleaned['Product'].fillna('Unknown')

# 3. 將 Customer 缺失值填補為 'Guest'
df_cleaned['Customer'] = df_cleaned['Customer'].fillna('Guest')

print("填補缺失值後的資料：")
print(df_cleaned.head(10))
```

### 如何自動化清理

定義標準化流程：</br>
輸入原始檔 -> 自動檢查問題 -> 執行清洗邏輯 -> 輸出報表</br>

實作邏輯
1. 輸入 (Input)：設定一個資料夾路徑，程式自動抓取該路徑下所有的 .csv 檔。
2. 自動檢查 (Check)：
    - df.isnull().sum()：自動統計每一欄有多少空值。
    - df.dtypes：檢查資料型態是否符合預期（如：金額應為數字而非字串）。
3. 執行清洗 (Clean)：呼叫預先寫好的清洗函數。
4. 輸出報表 (Output)：使用 to_csv() 匯出清洗後的檔案，並用 print() 輸出簡單的清洗日誌（Log）。

### AI 協作思維：運用 Gemini 生成邏輯
試著下Prompt讓 Gemini 幫忙寫出複雜的判斷式。</br>

操作場景：處理複雜的「金額」欄位</br>
假設資料中的金額長這樣："$ 25,000" 或 "25000元"，這無法直接計算。</br>
```
import pandas as pd

# 模擬包含各種「髒金額」的資料
data = {
    'OrderID': [101, 102, 103, 104, 105, 106],
    'Product': ['Laptop', 'Mouse', 'Keyboard', 'Monitor', 'Tablet', 'Webcam'],
    'Amount': ['$ 25,000', '800元', '$ 1,200', '5000', '12,000元', '未知'] # 包含各種雜訊
}

df = pd.DataFrame(data)

print("--- 清理前的原始資料 ---")
print(df)
print("\nAmount 欄位的資料型態:", df['Amount'].dtype)
```
對 Gemini 輸入以下 Prompt：</br>

「我有一欄 Pandas DataFrame 的資料叫 'Amount'，內容包含錢字號、逗號和中文字，例如 '$ 1,200' 或 '500元'。請幫我寫一個 Python 函數，可以自動把這些符號去掉，並將該欄位轉換為整數 (int)，如果遇到無法轉換的內容就填入 0。」</br>

Gemini 會產出類似下方的代碼：</br>
```
def clean_currency(value):
    if isinstance(value, str):
        # 移除 $, 逗號, 和 "元"
        clean_str = value.replace('$', '').replace(',', '').replace('元', '').strip()
        return int(clean_str) if clean_str.isdigit() else 0
    return value

# 自動化套用
df['Amount'] = df['Amount'].apply(clean_currency)
```
執行提供的 clean_currency 函數。透過 apply() 方法，可以將複雜的字串處理邏輯一次性套用到整欄資料。
```
def clean_currency(value):
    if isinstance(value, str):
        # 移除 $, 逗號, 和 "元"
        # 提示學員：這部分邏輯就是透過 Gemini 描述需求後生成的
        clean_str = value.replace('$', '').replace(',', '').replace('元', '').strip()
        
        # 判斷剩餘字串是否為數字，若不是（例如「未知」）則轉為 0
        return int(clean_str) if clean_str.isdigit() else 0
    return value

# 執行清理
df['Amount'] = df['Amount'].apply(clean_currency)

print("--- 清理後的資料 ---")
print(df)
print("\nAmount 欄位的資料型態:", df['Amount'].dtype)
```
驗證與進階計算
清理後的 Amount 已經變成 int64（整數）型態，這時候就可以進行數學運算了。
```
# 驗證是否可以計算總和
total_sales = df['Amount'].sum()
print(f"清理成功！總銷售金額為：{total_sales}")
```

### 練習1
如果金額是浮點數（例如 $ 1,200.50），原本的 .isdigit() 會失效，該如何叫 Gemini 改寫？
### 練習2
如果想要保留原始金額，並把清理後的結果放在新欄位 Amount_Cleaned 呢？

## 實作開發 - 自動化機器人
1. 建立偵測機制
```
import pandas as pd
import numpy as np

data = {
    '訂單編號': ['A001', 'A002', 'A002', 'A003', 'A004', 'A005', 'A006', 'A007', 'A008', 'A009',
             'A010', 'A011', 'A012', 'A013', 'A014', 'A015', 'A016', 'A017', 'A018', 'A019'],
    '日期': ['2024-01-01', '2024/01/02', '2024/01/02', '2024.01.03', '2024-01-04', '2024/01/05',
           '2024-01-06', '2024-01-07', '2024/01/08', '2024.01.09', '2024-01-10', '2024-01-11',
           '2024-01-12', '2024-01-13', '2024/01/14', '2024-01-15', '2024/01/16', '2024-01-17', '2024.01.18', '2024-01-19'],
    '客戶姓名': ['王小明', '李 華', '李 華', '張三', '陳小姐', '林大山', '黃阿寶', '周杰', '郭台', '趙六',
             '孫五', '錢七', '李 華', '吳小龍', '鄭總', '王小明', '馬雲', '劉強', '曾美麗', '林大山'],
    '產品類別': ['電子產品', '家電', '家電', '電子產品', np.nan, '生活用品', '電子產品', '生活用品', '家電', '電子產品',
             '生活用品', '家電', '家電', '電子產品', '生活用品', '電子產品', np.nan, '生活用品', '家電', '生活用品'],
    '銷售額': [1500, -500, -500, 3000, 1200, 800, 2500, -100, 5000, 1800,
            900, 4500, 500, 2100, 1300, 1500, 6000, 2200, -300, 850],
    '數量': [1, 2, 2, np.nan, 1, 3, 2, 1, 5, 2,
           1, 4, 1, 2, 1, 1, 6, 2, 1, 3],
    '電話號碼': ['0912345678', '0922-333-444', '0922-333-444', '0933111222', '02-23456789', '0955666777',
             '0911 222 333', '0988000111', '0977-123-123', '0966444555', '0900111222', '0227654321',
             '0922-333-444', '0933000999', '0911111222', '0912345678', '0955555444', '0922888777', '0911000222', '0955666777'],
    '備註': [None, '重複', '重複', None, '缺類別', None, None, '異常', None, None,
           None, None, '回購', None, None, '老客戶', '缺類別', None, '異常', None]
}

df = pd.DataFrame(data)
df.to_csv('dirty_data.csv', index=False, encoding='utf-8-sig')
print("髒資料檔案 'dirty_data.csv' 已生成！")
```
- 欄位類型偵測：df.dtypes。「日期」可能是 Object (字串)，而不是 Datetime。
```
print(df.dtypes)
```

- 缺失值熱點圖
```
print(df.isnull().sum()) # 快速查看每一欄有多少空格
```

- 重複資料偵測
```
duplicate_rows = df[df.duplicated()]
print(f"發現 {len(duplicate_rows)} 筆重複資料")
```

2. 模組化清洗邏輯
寫 def 函數，將原本雜亂的程式碼封裝成模組。

- 標準化日期：統一處理 -、/、. 的日期格式。
- 數值邏輯修正：運用 .abs() 修正負數銷售額（或定義為異常並移除）。
- 字串標準化：移除客戶姓名中的空格，並統一電話號碼（移除橫線與空格）。

提示詞：</br>

有一個 Pandas 的 DataFrame，其中『日期』欄位的格式很亂，包含 2024-01-01、2024/01/02 和 2024.01.03。寫一個 Python 函數，將這些不同的格式統一轉換為標準的 YYYY-MM-DD 日期格式，若遇到無法轉換的內容請設為空值。</br>
```
import pandas as pd

def clean_date_format(input_df, column_name='日期'):
    """
    自動化日期清洗機器人
    1. 統一將分隔符 (. /) 轉換為標準的 (-)
    2. 強制轉換為標準 Datetime 物件
    3. 報表化處理結果
    """
    df = input_df.copy()

    # 確保欄位存在
    if column_name in df.columns:
        # 轉為字串並統一分隔符號
        df[column_name] = df[column_name].astype(str).str.replace('.', '-', regex=False)
        df[column_name] = df[column_name].str.replace('/', '-', regex=False)

        # 轉換為日期型別，無法解析的會變成 NaT (空日期)
        df[column_name] = pd.to_datetime(df[column_name], errors='coerce')

        # 輸出處理日誌
        nat_count = df[column_name].isna().sum()
        print(f"✅ '{column_name}' 欄位處理完成！")
        if nat_count > 0:
            print(f"⚠️ 注意：有 {nat_count} 筆資料因格式過於混亂無法解析，已轉為 NaT。")

    return df
```
提示詞：</br>

提供驗證程式碼</br>
```
# 1. 執行清洗
cleaned_df = clean_date_format(df)

# 2. 驗證結果
print("\n--- 清洗後日期檢查 ---")
print(cleaned_df['日期'])

# 3. 檢查型別 (正確應為 datetime64[ns])
print(f"\n目前資料型別: {cleaned_df['日期'].dtype}")
```


3. 自動化輸出與日誌
